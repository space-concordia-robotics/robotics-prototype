cmake_minimum_required(VERSION 3.5.1)
project(robotics-prototype)
set(modules Science)
foreach(module ${modules})
        add_subdirectory(${module})
        message("Building ${module}")
endforeach()
